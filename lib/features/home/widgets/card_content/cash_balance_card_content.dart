import 'package:flutter/material.dart';

class CashBalanceCardContent extends StatelessWidget {
  const CashBalanceCardContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}