import 'package:flutter/material.dart';

class GoalWalletFilled extends StatelessWidget {
  const GoalWalletFilled({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}